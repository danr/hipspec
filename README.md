Haskell to Why3 translation
===========================

If the file name is `Test` and the property
is called `prop_test`, run it as so:

    hipspec Test.hs --only=prop_test --print-why3 --translate-only

